angular
  .module("app")
  .controller("RonMeController", RonMeController)

function RonMeController($http, $stateParams, $state) {
  const vm = this
    //base urL?

  vm.$onInit = function() {
  }
}
