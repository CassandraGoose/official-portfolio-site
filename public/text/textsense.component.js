angular
  .module("app")
  .controller("TextSenseController", TextSenseController)

function TextSenseController($http, $stateParams, $state) {
  const vm = this
    //base urL?

  vm.$onInit = function() {
  }
}
